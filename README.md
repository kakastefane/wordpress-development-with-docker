# wordpress-development-with-docker
Aprendendo a criar containers no docker para desenvolver em wordpress.

Para rodar o wordpress no docker a primeira vez
sudo docker-compose -f "docker-compose.yml" up -d --build

Para parar
sudo docker-compose stop

E para Iniciar
sudo docker-compose start
